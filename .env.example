# Supabase Configuration
# Get these values from your Supabase project settings (https://app.supabase.com)

# Public Environment Variables (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Private Environment Variables (NEVER expose to the browser)
# Only use in server-side code and Edge Functions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: JWT Secret (if you need to verify tokens manually)
# SUPABASE_JWT_SECRET=your-jwt-secret
